CREATE TABLE car (
                     Id serial PRIMARY KEY,
                     make VARCHAR(255),
                     model VARCHAR(255),
                     price DECIMAL(10, 2),
                     year INTEGER
);
